# Datenbank-Passwort
DB_PASSWORD=changeme

# Pfad zum Quell-Verzeichnis mit den Audiobook-ZIPs
SOURCE_DIR=/opt/audiobook-storage/zips

# Pfad zum Export-Verzeichnis
EXPORT_DIR=/opt/audiobook-storage/export

# Pfad zum Storage-Verzeichnis (toc/, covers/, pdf/ ...)
STORAGE_DIR=/opt/dav-storage

# JWT-Geheimschlüssel (langer, zufälliger String)
JWT_SECRET=change-me-in-production

# Benutzer-Passwörter (Web-App Login)
USER_BERND_PASSWORD=Bx7@kP3mLw
USER_DORO_PASSWORD=Qn5!sJ7vYe

# SFTP-Upload-Server
# Port auf dem der SFTP-Server lauscht (Standard: 2222)
SFTP_PORT=2222
# SSH Public Key in config/sftp_authorized_keys eintragen (kein Passwort)
